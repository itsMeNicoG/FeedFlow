# FeedFlow Environment Variables - EJEMPLO
# ⚠️ Copia este archivo a .env y configura tus valores

# JWT Secret - Clave secreta para firmar tokens de autenticación
# Genera uno nuevo con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu-secreto-super-largo-y-aleatorio-aqui

# Database Path
DB_PATH=feedflow.sqlite

# Server Port
PORT=3000
